import React from 'react';
import RetailReportsPage from './RetailReportsPage'; // Re-use for now as structure is similar

const ServiceReportsPage = (props) => {
    return <RetailReportsPage {...props} />;
};

export default ServiceReportsPage;