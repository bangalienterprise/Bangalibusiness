import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.bangalienterprise.app',
  appName: 'Bangali Enterprise',
  webDir: 'dist'
};

export default config;
